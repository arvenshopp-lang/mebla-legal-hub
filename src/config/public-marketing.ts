/**
 * أعلام العرض للصفحات العامة (Marketing/Public) — مصدر مركزي واحد.
 *
 * القاعدة: أي قدرة غير معتمدة للإطلاق العام تُخفى من الواجهة العامة فقط،
 * دون حذف كودها أو مساراتها أو خدماتها الداخلية.
 * تفعيل أي علم هنا يتطلب موافقة مالك المنصة.
 */

/** مداخل «المحامية بيان» التسويقية والمساعد العائم في الصفحات العامة. */
export const PUBLIC_BAYAN_MARKETING_ENABLED = false;

/**
 * شعارات وسائل الدفع في الصفحات العامة.
 * تبقى مغلقة حتى يُفعَّل مزود دفع فعلي ويُختبر في الإنتاج.
 */
export const PUBLIC_PAYMENT_LOGOS_ENABLED = false;

/**
 * زر «إعدادات ملفات تعريف الارتباط» في التذييل.
 * لا يوجد حالياً مركز تفضيلات مركّب على الصفحات العامة يستجيب لحدث الفتح،
 * فلا يجوز إظهار زر بلا وظيفة.
 */
export const PUBLIC_COOKIE_PREFS_ENABLED = false;

/** رابط مركز المساعدة `/docs` في التذييل — يعود عند توفر مقالات دعم فعلية. */
export const PUBLIC_DOCS_LINK_ENABLED = false;
